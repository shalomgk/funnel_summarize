"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkflowErrorHandler = void 0;
const logger_1 = require("./logger");
// TODO: Improve error handling
class WorkflowErrorHandler {
    static handle(error) {
        if (error.response) {
            const status = error.response.status;
            const data = error.response.data;
            switch (true) {
                case status >= 200 && status < 300:
                    this.logger.info('Successful response:', data);
                    break;
                case status >= 400 && status < 500:
                    this.logger.error('Client-side Error:', data);
                    break;
                case status >= 500 && status < 600:
                    this.logger.error('Server-side Error:', data);
                    break;
                default:
                    this.logger.error('HTTP Error:', data);
                    break;
            }
        }
        else {
            this.logger.error('Network error:', error.message);
        }
    }
}
exports.WorkflowErrorHandler = WorkflowErrorHandler;
WorkflowErrorHandler.logger = (0, logger_1.getLogger)('WorkflowErrorHandler', logger_1.LogLevel.ERROR);
//# sourceMappingURL=errors.js.map