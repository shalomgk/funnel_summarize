import { Skill } from './skill';
export declare class AgentHandler {
    Goal: string;
    Guidance: string;
    Skills: Skill[];
    token: string;
    endpoint: string;
    constructor(endpoint: string, token: string, goal?: string, guidance?: string, skills?: Skill[]);
    InstallAgent(): Promise<any>;
    UpdateAgent(agent: string, newGoal: string, newGuidance: string): Promise<any>;
    Execute(input: string, agentId: string, sessionObjectId: string): Promise<string>;
    private executeEvent;
}
